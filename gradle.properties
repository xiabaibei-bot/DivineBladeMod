# Gradle配置
org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true

# 模组版本配置
minecraft_version=1.20.1
neo_version=20.4.179
mod_version=1.0.0
mod_id=divineblade

# 发布配置
version=${mod_version}
group=com.example.divineblade

# 以下两个属性很重要
org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=1G
